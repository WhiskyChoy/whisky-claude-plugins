---
description: Remove the claude-statusline configuration from settings
---

# Uninstall Claude Statusline

Remove the statusline configuration from the user's Claude Code settings.

## Instructions

1. Read `~/.claude/settings.json`
2. Remove the `statusLine` field entirely
3. Write the updated settings back
4. Tell the user the statusline has been removed and will take effect on next restart
